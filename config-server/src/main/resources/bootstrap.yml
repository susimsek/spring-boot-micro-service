# resource dizinide microservice klasörünün altında arar.
# profile ayarı yaptık
server:
  port: 8888
spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/microservice
